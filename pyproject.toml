[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pymvn"
version = "1.0.1"
dependencies = [
    "requests",
    "beautifulsoup4",
    "lxml",
]
requires-python = ">=3.12"

[tool.hatch.build]
exclude = [
    "/.*",
    "/out",
]